os:
  - osx
language: generic
sudo: required
dist: trusty
osx_image: xcode8.3
script:
  - swift package clean
  - swift build
  - swift test -Xswiftc -DCI_BUILD
notifications:
  email:
    on_success: never
    on_failure: change
